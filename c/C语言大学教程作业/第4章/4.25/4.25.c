#include<stdio.h>

void main(void)
{
    int counter,decimal,binary,octonary,hexadecimal;
    printf("decimal   binary   octonary   hexadecimal\n");
    for(counter=1;counter<=256;counter++)
    {
        decimal=counter;
        decimal=counter;
        int yushu1;
        while(yushu1!=0)
        {   int n1;
            n1=decimal(int n1,int decimal);
        }
        switch()
    }
    printf("%10d%9d%11d%14d")
}
int decimal(int n,int counter1)
{
    n=0;
    if(counter1>=pow(2,n))
    {
        n++;
    }
    n=n-1;
}
